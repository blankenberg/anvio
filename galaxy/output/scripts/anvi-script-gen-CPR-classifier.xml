<tool id="anvi_script_gen_CPR_classifier" name="anvi-script-gen-CPR-classifier" version="5.2">
    <description>Train a classifier for CPR prediction</description>
    <requirements>
        <requirement type="package" version="5.2">anvio</requirement>
    </requirements>
    <stdio>
        <exit_code range="1:" />
    </stdio>
    <version_command>anvi-script-gen-CPR-classifier --version</version_command>
    <command><![CDATA[
        anvi-script-gen-CPR-classifier

#if $str( $matrix ):
     '${matrix}'
#end if


#if $str( $output ):
    --output '${output}'
#end if

&> '${GALAXY_ANVIO_LOG}'

 &&
ls -lahR
    ]]></command>
    <inputs>
        <param name="matrix" type="text" label="Matrix" value="" optional="True" argument="" help='TAB-delimited matrix of CPR genome names, classes, and presence absence of single-copy genes. Headers of the first two rows should be "genome", and "class". The rest of the rows shold be single-copy genes.'/>
    </inputs>
    <outputs>
        
        <data name="GALAXY_ANVIO_LOG" format="txt" label="${tool.name} on ${on_string}: Log"/>
    </outputs>
    <help><![CDATA[
        usage: anvi-script-gen-CPR-classifier [-h] [-o OUTPUT] matrix

Train a classifier for CPR prediction

positional arguments:
  matrix                TAB-delimited matrix of CPR genome names, classes, and
                        presence absence of single-copy genes. Headers of the
                        first two rows should be "genome", and "class". The
                        rest of the rows shold be single-copy genes.

optional arguments:
  -h, --help            show this help message and exit
  -o OUTPUT, --output OUTPUT
                        Output file name for the classifier.

    ]]></help>
    <citations>
        <citation type="doi">10.7717/peerj.1319</citation>
    </citations>
</tool>