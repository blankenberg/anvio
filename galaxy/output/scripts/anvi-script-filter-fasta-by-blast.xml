<tool id="anvi_script_filter_fasta_by_blast" name="anvi-script-filter-fasta-by-blast" version="5.3.0">
    <description>Filter FASTA file according to BLAST table (remove sequences with bad
    BLAST alignment.</description>
    <requirements>
        <requirement type="package" version="5.3.0">anvio</requirement>
    </requirements>
    <stdio>
        <exit_code range="1:" />
    </stdio>
    <version_command>anvi-script-filter-fasta-by-blast --version</version_command>
    <command><![CDATA[
        anvi-script-filter-fasta-by-blast

#if $fasta_file:
    --fasta-file '${fasta_file}'
#end if


#if $output_file:
    --output-file '${output_file}'
#end if

--blast-output '${blast_output}'

--outfmt '${outfmt}'

--threshold '${threshold}'

--just-do-it
&> '${GALAXY_ANVIO_LOG}'

    ]]></command>
    <inputs>
        <param name="fasta_file" type="data" label="Fasta File" format="fasta" optional="True" multiple="False" argument="--fasta-file" help="A FASTA-formatted input file"/>
        <param name="blast_output" type="data" label="Blast Output" format="tabular" optional="False" multiple="False" argument="--blast-output" help="BLAST table generated with blastp. `--outfmt 6` as the output format is assumed."/>
        <param name="outfmt" type="text" label="Outfmt" value="" optional="False" argument="--outfmt" help="Specify the column ordering of your BLAST report. We add the following paramter to our BLAST searches so the output report contains the `qlen` field, which is not included by default: `-outfmt '6 qseqid sseqid pident length mismatch gapopen qstart qend sstart send evalue bitscore qlen slen'`. You may have used a different `-outfmt` paramter, and you should use this parameter to explicitly define the column names in your output file. For instance, if you had used the parameter mentioned above, then the correct version of this parameter would be: &quot;qseqid sseqid pident length mismatch gapopen qstart qend sstart send evalue bitscore qlen slen&quot;. Regardless of the BLAST output format, your columns MUST contain the following parameters for this program to work properly: 'qseqid', 'bitscore', 'length', 'qlen', and 'pident'."/>
        <param name="threshold" type="text" label="Threshold" value="30.0" optional="False" argument="--threshold" help="What `proper_pident` threshold do you want to use for filtering out sequences whose top bit-score matches have `proper_pident`s less than this threshold? We have defined `proper_pident` to be the percentage of the query amino acids that both aligned to and were identical to the corresponding matched amino acid. Note that the `pident` parameter output by BLAST does not include regions of the query sequence unaligned to the matched sequence, whereas `proper_pident` does. For example, a sequence that's only half aligned by a match but with 100%% identity at matched regions has a `pident` of 100 but a `proper_pident` of 50. The default is %(default).1f%%."/>
    </inputs>
    <outputs>
        <data name="output_file" format="txt" format_source="input_output_file" metadata_source="input_output_file" label="${tool.name} on ${on_string}: Output File"/>
        <data name="GALAXY_ANVIO_LOG" format="txt" label="${tool.name} on ${on_string}: Log"/>
    </outputs>
    <help><![CDATA[
        usage: anvi-script-filter-fasta-by-blast [-h] [-f FASTA] [-o FILE_PATH] -b TAB DELIMITED
                              FILE -s OUTFMT -t THRESHOLD [--just-do-it]

Filter FASTA file according to BLAST table (remove sequences with bad BLAST
alignment.

optional arguments:
  -h, --help            show this help message and exit
  -f FASTA, --fasta-file FASTA
                        A FASTA-formatted input file
  -o FILE_PATH, --output-file FILE_PATH
                        File path to store results.
  -b TAB DELIMITED FILE, --blast-output TAB DELIMITED FILE
                        BLAST table generated with blastp. `--outfmt 6` as the
                        output format is assumed.
  -s OUTFMT, --outfmt OUTFMT
                        Specify the column ordering of your BLAST report. We
                        add the following paramter to our BLAST searches so
                        the output report contains the `qlen` field, which is
                        not included by default: `-outfmt '6 qseqid sseqid
                        pident length mismatch gapopen qstart qend sstart send
                        evalue bitscore qlen slen'`. You may have used a
                        different `-outfmt` paramter, and you should use this
                        parameter to explicitly define the column names in
                        your output file. For instance, if you had used the
                        parameter mentioned above, then the correct version of
                        this parameter would be: "qseqid sseqid pident length
                        mismatch gapopen qstart qend sstart send evalue
                        bitscore qlen slen". Regardless of the BLAST output
                        format, your columns MUST contain the following
                        parameters for this program to work properly:
                        'qseqid', 'bitscore', 'length', 'qlen', and 'pident'.
  -t THRESHOLD, --threshold THRESHOLD
                        What `proper_pident` threshold do you want to use for
                        filtering out sequences whose top bit-score matches
                        have `proper_pident`s less than this threshold? We
                        have defined `proper_pident` to be the percentage of
                        the query amino acids that both aligned to and were
                        identical to the corresponding matched amino acid.
                        Note that the `pident` parameter output by BLAST does
                        not include regions of the query sequence unaligned to
                        the matched sequence, whereas `proper_pident` does.
                        For example, a sequence that's only half aligned by a
                        match but with 100% identity at matched regions has a
                        `pident` of 100 but a `proper_pident` of 50. The
                        default is 30.0%.
  --just-do-it          Don't bother me with questions or warnings, just do
                        it.

    ]]></help>
    <citations>
        <citation type="doi">10.7717/peerj.1319</citation>
        <citation type="bibtex">@ARTICLE{Blankenberg19-anvio,
   author = {Daniel Blankenberg, et al},
   title = {In preparation..},
   }</citation>
    </citations>
</tool>